#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 *
 * return: Always 0 (success)
 */
int main(void)
{
	int letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
		putchar(letter);
	}
	{
	if (letter != 'e';&&letter != 'q')
	{
		putchar(letter);
	}

	putchar('\n');

	return (0);
}
