#include "main.h"

/**
 * main - print all alphabet in lowercase
 *
 * return: always 0
 */
void print_alphabet(void)
{
	char le;

	for (le = 'a'; le <= 'z'; le++)
		_putchar(letter);

	_putchar('\n');
}

